# Czech Speech Transcription Requirements
# Core dependencies for OpenAI Whisper and audio processing

# OpenAI Whisper for speech-to-text transcription
openai-whisper>=20231117

# Audio processing and format conversion
pydub>=0.25.1

# Additional audio/video processing backend (required for MP3 support)
ffmpeg-python>=0.2.0

# Torch for Whisper model execution (CPU/GPU support)
torch>=2.0.0
torchaudio>=2.0.0

# NumPy for numerical operations (Whisper dependency)
numpy>=1.24.0

# Transformers library (Whisper dependency)
transformers>=4.35.0

# Additional utilities
pathlib2>=2.3.7; python_version < "3.4"

# Optional: For CUDA GPU acceleration (uncomment if you have NVIDIA GPU)
# torch>=2.0.0+cu118
# torchaudio>=2.0.0+cu118
